<ul class="navbar-nav mr-auto">
  <% if can? :navigate, :ad_users %>
    <%= link_to t('controller.ad_users'), main_app.ad_users_path,
        class: "nav-link #{active_class('ad_users')}" %>
  <% end %>
</ul>
